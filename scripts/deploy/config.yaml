# Cluster node IPs (used for gossip + pdsh)
nodes:
  - 10.0.0.84
  - 10.0.0.85
  - 10.0.0.86
  - 10.0.0.87
  - 10.0.0.88

# Gossip configuration
gossip:
  port_range:
    start: 10010
    end: 10020

# Raft / service ports
ports:
  peer: 10000
  client: 10005

# Base directory for all ctlplane data on each node
base_dir: /work/ctlplane
